#!/usr/bin/env bash
# Stop hook: reject a turn that broke bequiet. Silent and token-free when clean.

set -uo pipefail

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
. "${SCRIPT_DIR}/pyfind.sh"
PY="$(shut_py)" || exit 0
exec "$PY" "${SCRIPT_DIR}/stop_gate.py"
